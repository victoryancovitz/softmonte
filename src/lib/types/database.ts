/**
 * Supabase Database Types
 *
 * To regenerate, run:
 *   npx supabase gen types typescript --project-id wzmkifutluyqzqefrbpp > src/lib/types/database.ts
 *
 * These types are used to provide type safety for Supabase queries.
 */

// Placeholder — run the command above to generate actual types
export type Database = Record<string, any>
